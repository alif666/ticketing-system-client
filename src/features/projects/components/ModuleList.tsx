import { Layers3, Pencil, Trash2 } from "lucide-react";
import type { Module } from "../types";
import { Pagination } from "./Pagination";

export function ModuleList({ modules, canManage, onEdit, onDelete, page, totalPages, onPageChange }: { modules: Module[]; canManage: boolean; onEdit: (id: number) => void; onDelete: (id: number) => void; page: number; totalPages: number; onPageChange: (page: number) => void }) {
  if (!modules.length) return <div className="rounded-lg border border-dashed p-8 text-center"><Layers3 className="mx-auto h-7 w-7 text-muted-foreground" /><p className="mt-3 font-medium">No modules yet</p><p className="mt-1 text-sm text-muted-foreground">Create a module to organize this project’s issues.</p></div>;
  return <><div className="grid gap-3 sm:grid-cols-2">{modules.map((module) => <div key={module.id} className="rounded-lg border p-4"><div className="flex items-start justify-between gap-3"><div><div className="flex items-center gap-2"><p className="font-medium">{module.name}</p><span className={`text-[10px] font-bold uppercase ${module.active ? "text-emerald-700" : "text-muted-foreground"}`}>{module.active ? "Active" : "Inactive"}</span></div><p className="mt-1 text-sm text-muted-foreground">{module.description || "No description"}</p></div>{canManage && <div className="flex"><button type="button" aria-label={`Edit ${module.name}`} onClick={() => onEdit(module.id)} className="rounded-md p-2 text-muted-foreground hover:bg-muted hover:text-foreground"><Pencil className="h-4 w-4" /></button><button type="button" aria-label={`Delete ${module.name}`} onClick={() => onDelete(module.id)} className="rounded-md p-2 text-muted-foreground hover:bg-red-50 hover:text-red-700"><Trash2 className="h-4 w-4" /></button></div>}</div></div>)}</div><Pagination page={page} totalPages={totalPages} onChange={onPageChange} /></>;
}
